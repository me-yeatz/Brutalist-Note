---
// Hero section
---

<section class="hero">
  <div class="hero-content">
    <div class="hero-badge">
      <span>⚡</span>
      <span class="hero-badge-text">Zero Fluff. Maximum Productivity.</span>
    </div>
    <h1 class="hero-title">
      Stop Taking <span class="hero-highlight">Wimpy</span> Notes
    </h1>
    <p class="hero-subtitle">
      A no-nonsense, Neo-Brutalist note-taking app. Zero fluff. Maximum productivity. Works offline on any device.
    </p>
    <div class="hero-buttons">
      <a href="/app" class="btn-primary">Launch App</a>
      <a href="https://github.com/me-yeatz/brutalist-note-astro" target="_blank" class="btn-secondary">View Source</a>
    </div>
  </div>
  <div class="hero-image">
    <div class="hero-demo">
      <div class="demo-header">
        <div class="demo-dot"></div>
        <div class="demo-dot"></div>
        <div class="demo-dot"></div>
        <span class="demo-title">brutal-notes.app</span>
      </div>
      <div class="demo-content">
        <div class="demo-note">
          <div class="note-header">📝 My Brutal Notes</div>
          <div class="note-line"></div>
          <div class="note-line short"></div>
          <div class="note-line"></div>
          <div class="note-line medium"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
    padding: 120px 64px 64px;
    max-width: 1400px;
    margin: 0 auto;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--black);
    color: var(--yellow);
    padding: 12px 20px;
    border: 3px solid var(--black);
    width: fit-content;
    font-weight: 700;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .hero-badge-text {
    color: var(--yellow);
  }

  .hero-title {
    font-size: 72px;
    font-weight: 800;
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: -2px;
    color: var(--black);
  }

  .hero-highlight {
    color: var(--orange);
    position: relative;
  }

  .hero-subtitle {
    font-size: 20px;
    line-height: 1.6;
    color: var(--black);
    opacity: 0.8;
    max-width: 540px;
  }

  .hero-buttons {
    display: flex;
    gap: 16px;
    margin-top: 16px;
  }

  .btn-primary {
    background: var(--orange);
    color: var(--white);
    border: 4px solid var(--black);
    padding: 18px 36px;
    font-weight: 800;
    font-size: 16px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-decoration: none;
    transition: all 0.2s ease;
    display: inline-block;
    box-shadow: 6px 6px 0 var(--black);
  }

  .btn-primary:hover {
    background: var(--yellow);
    transform: translate(-2px, -2px);
    box-shadow: 8px 8px 0 var(--black);
  }

  .btn-secondary {
    background: var(--white);
    color: var(--black);
    border: 4px solid var(--black);
    padding: 18px 36px;
    font-weight: 800;
    font-size: 16px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-decoration: none;
    transition: all 0.2s ease;
    display: inline-block;
    box-shadow: 6px 6px 0 var(--black);
  }

  .btn-secondary:hover {
    background: var(--black);
    color: var(--white);
    transform: translate(-2px, -2px);
    box-shadow: 8px 8px 0 var(--black);
  }

  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-demo {
    background: var(--white);
    border: 4px solid var(--black);
    box-shadow: 16px 16px 0 var(--black);
    width: 100%;
    max-width: 500px;
  }

  .demo-header {
    background: var(--black);
    padding: 16px;
    display: flex;
    align-items: center;
    gap: 8px;
    border-bottom: 4px solid var(--black);
  }

  .demo-dot {
    width: 16px;
    height: 16px;
    border-radius: 50%;
    border: 3px solid var(--orange);
  }

  .demo-dot:nth-child(2) {
    border-color: var(--yellow);
  }

  .demo-dot:nth-child(3) {
    border-color: var(--white);
  }

  .demo-title {
    color: var(--white);
    font-size: 12px;
    font-weight: 600;
    margin-left: 12px;
    text-transform: uppercase;
  }

  .demo-content {
    padding: 32px;
  }

  .demo-note {
    background: var(--gray);
    border: 3px solid var(--black);
    padding: 24px;
  }

  .note-header {
    font-size: 18px;
    font-weight: 800;
    margin-bottom: 20px;
    text-transform: uppercase;
  }

  .note-line {
    height: 12px;
    background: var(--black);
    margin-bottom: 12px;
  }

  .note-line.short {
    width: 60%;
  }

  .note-line.medium {
    width: 80%;
  }

  @media (max-width: 1024px) {
    .hero {
      grid-template-columns: 1fr;
      padding: 100px 32px 32px;
      gap: 48px;
    }

    .hero-title {
      font-size: 56px;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 100px 20px 32px;
    }

    .hero-title {
      font-size: 42px;
    }

    .hero-subtitle {
      font-size: 18px;
    }

    .hero-buttons {
      flex-direction: column;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      text-align: center;
    }
  }
</style>
